import * as fs from 'fs';
import * as path from 'path';
const unusedConfig = { debug: true };
const userName: string = 42;
function processData(data: string, count: number): string { const x: string = 123; return 42; }
export { processData };
